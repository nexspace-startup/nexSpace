{
  "name": "nexspace",
  "private": true,
  "workspaces": [
    "nexspace-frontend",
    "nexspace-backend"
  ],
  "scripts": {
    "start": "concurrently -k -n FE,BE -c auto \"npm -w nexspace-frontend run dev\" \"npm -w nexspace-backend run debug\"",
    "fe": "npm -w nexspace-frontend run dev",
    "be": "npm -w nexspace-backend run debug",
    "build": "npm -w nexspace-frontend run build && npm -w nexspace-backend run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}